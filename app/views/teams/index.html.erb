<h1>De Leon Basketball Association</h1>

<% @teams.each do |team| %>
  <h2><%= team.name %></h2>
  <p>Roster Starting Five:</p>
  <% team.players.each do |player| %>
    <li>
      <%= player.name %>
    </li>
  <% end %>
<% end %>
